coverage:
  status:
    project:
      default:
        target: 0%
        threshold: 0%
    patch:
      default:
        target: 0%
        threshold: 0%

ignore:
  - "**/*.g.dart"

codecov:
  require_ci_to_pass: false
  require_changes: no
