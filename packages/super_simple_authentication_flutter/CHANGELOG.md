## 0.0.1-dev.3

- Add password reset functionality
  - Add `sendPasswordResetEmail()` method to send password reset emails
  - Add `resetPassword()` method to reset passwords with tokens
  - Add `PasswordResetException` and `PasswordResetError` types
  - Update endpoint paths to include password reset routes

## 0.0.1-dev.2

- Fixed sign in error enum

## 0.0.1-dev.1

- Initial release!
